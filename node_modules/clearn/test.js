var module = require('./build/Release/clearn');

//module.varAddr();

//module.arrayAddr();

//module.structAddr();

//module.stringTest();

module.objectArrayTest();


var count = 0;
while(count < 1000000)
{
    module.objectArrayTest();
    count++;
}
