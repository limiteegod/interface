#ifndef VS_STRING_H
#define VS_STRING_H

namespace vs {

class String {

    public:
        String();
        String(int volume, const char *src);
        ~String();

        void show();
        int getLength();
        void append(const char *src);

    private:
        int length; //当前长度
        int volume; //当前的最大容量
        char *p;    //初始的地址
};

}

#endif