#ifndef SINGLELINKEDLIST_H
#define SINGLELINKEDLIST_H

#include "SingleLinkedNode.h"

class SingleLinkedList {

    public:
        SingleLinkedList();
        ~SingleLinkedList();

        SingleLinkedNode* findById(int id);
        SingleLinkedNode* deleteById(int id);
        void insert(SingleLinkedNode* node);
        void traverse();

    private:
        SingleLinkedNode *head;
        void init();
};


#endif