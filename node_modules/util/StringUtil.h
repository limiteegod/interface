#ifndef STRINGUTIL_H
#define STRINGUTIL_H

class StringUtil {
    public:
        StringUtil();
        ~StringUtil();

        static void Copy(const char *src, char *dest);
        static int Length(const char *src);

    private:
};

#endif